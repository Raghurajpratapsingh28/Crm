import { Placeholder } from "../placeholder";

export default function Page() {
  return (
    <Placeholder
      title="Leads"
      note="Leads are deals in the first pipeline stage — one unified record, not a separate table."
    />
  );
}
