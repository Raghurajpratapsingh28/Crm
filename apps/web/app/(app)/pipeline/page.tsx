import { Placeholder } from "../placeholder";

export default function Page() {
  return (
    <Placeholder
      title="Pipeline"
      note="Kanban by pipeline_stages. Dropping into Lost requires a reason. Refresh-on-focus for v1."
    />
  );
}
